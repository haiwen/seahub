define([
    'jquery',
    'underscore',
    'backbone',
    'common'
], function($, _, Backbone, Common) {
    'use strict';

    var GroupsView = Backbone.View.extend({
        el: '#groups',

        events: {
            
        },

        initialize: function(options) {
            //里面会写group的东西
        },

        hide: function() {
            
        }

    });

    return GroupsView;
});
